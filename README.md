# Ivory-Goat
Source repository

This repository contains a full-stack web project built with **PHP**, **JavaScript**, **CSS**, **HTML**, and supporting **JSON** configuration/data files.  
It demonstrates secure server-side logic, dynamic client-side interactions, and polished UI/UX styling.

---

## 📂 Project Structure

- **`index.html`** → Entry point of the application  
- **`style.css`** → Global styles and responsive design rules  
- **`script.js`** → Client-side interactivity and DOM manipulation  
- **`config.json`** → Configuration or data used by the app  
- **`server.php`** → Backend logic, validation, and server-side processing  

---

## 🚀 Features

- **Server-side validation (PHP)**  
  Secure handling of user input, avoiding exposure of sensitive logic in client-side code.

- **Dynamic client-side interactions (JS)**  
  Real-time feedback, animations, and event-driven UI updates.

- **Responsive design (CSS)**  
  Mobile-friendly layouts with hover effects, transitions, and polished styling.

- **Structured data (JSON)**  
  Easy-to-maintain configuration and data exchange between client and server.

- **Semantic markup (HTML)**  
  Clean, accessible structure for content and layout.

---

## ⚙️ Installation & Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/ybiswasasit14-max/Ivory-Goat.git
   cd Ivory-Goat
